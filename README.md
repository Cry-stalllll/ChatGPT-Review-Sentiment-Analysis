csv file contains original dataset (raw, without polarity score) and modified dataset (cleaned , with polarity scored)
just run main.py (don't forget change the dataset file path and output file path)
